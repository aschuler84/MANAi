plugins {
    id 'java'
    id 'org.jetbrains.intellij' version '1.3.0'
    id 'io.ebean' version '12.12.1'
}

group 'org.example'
version '1.0-SNAPSHOT'

repositories {
    mavenLocal()
    mavenCentral()
}

apply plugin: 'maven'

dependencies {
    testCompile group: 'junit', name: 'junit', version: '4.12'
    compile 'io.ebean:ebean:12.12.1'
    annotationProcessor 'io.ebean:querybean-generator:12.12.1'
    implementation 'com.h2database:h2:2.0.202'
    implementation group: 'at.mana', name: 'template', version: "1.0-SNAPSHOT"
    implementation group: 'at.mana', name: 'visualisation', version: "1.0-SNAPSHOT"
    implementation group: 'at.mana', name: 'cli', version: "1.0-SNAPSHOT"
    compileOnly 'org.projectlombok:lombok:1.18.16'
    annotationProcessor 'org.projectlombok:lombok:1.18.16'
}

// See https://github.com/JetBrains/gradle-intellij-plugin/
intellij {
    version = '2021.2'
    plugins = ['java']
}

patchPluginXml {
    version = project.version
    sinceBuild = '203'
    untilBuild = '212.*'
}
