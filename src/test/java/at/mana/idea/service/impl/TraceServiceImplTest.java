package at.mana.idea.service.impl;

import at.mana.idea.domain.Sample;
import at.mana.idea.service.TraceService;
import com.google.gson.JsonArray;
import com.google.gson.JsonObject;
import org.junit.Assert;
import org.junit.Test;


public class TraceServiceImplTest {

    @Test
    public void testProcessTraces() {
        Assert.fail();
        //TraceService service = new TraceServiceImpl(null);
        // TODO: Prepare data for process - read json file and process data
        //Sample sample = null;
        //JsonObject root = null;
        //JsonArray children = null;
        //service.attributeTraces( sample, root, children);
    }

}
